<ul class="footer_menu">
    {% for item in items %}
            <li><a href="{% if item.slug is not null %}{{ item.slug }} {% else %}#{% endif %}"> {{ item.name }}</a></li>
    {% endfor %}
    <li><a href="/sitemap">Карта сайта</a> </li>
    <li><a href="#">О нас</a> </li>
    <li><a href="#">Контакты</a> </li>
</ul>